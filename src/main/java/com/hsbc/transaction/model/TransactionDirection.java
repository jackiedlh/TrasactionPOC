package com.hsbc.transaction.model;

public enum TransactionDirection {
    IN,    // For incoming transactions (deposits, received payments)
    OUT    // For outgoing transactions (withdrawals, sent payments)
} 